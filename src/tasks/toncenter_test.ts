// import { getTransactions } from '../services/toncenter'
// import { parseRawTransaction } from '../services/ton'

export default async (): Promise<void> => {
  console.log('need set address')
  // const results = await getTransactions()

  // results.forEach(raw => {
  //   const parsed = parseRawTransaction(raw)
  //   console.log({ ...parsed, body: '<hide>' })
  // })
}
