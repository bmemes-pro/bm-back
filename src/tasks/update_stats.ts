// import { dataSource } from '../db'
// import { Post } from '../db/entities/Post'
// import updatePostStats from '../services/viewlog/updatePostStats'

// const postsRepo = dataSource.getRepository(Post)

export default async (): Promise<void> => {
  // const post1 = await postsRepo.findOneBy({ id: 166 })
  // const post2 = await postsRepo.findOneBy({ id: 138 })

  // await updatePostStats(post1)
  // console.log('---')
  // await updatePostStats(post2)
}
